(include_subdirs unqualified)

(library
 (name xmas)
 (libraries ppx_show.runtime core)
 (preprocess
  (pps ppx_show))
 (flags
  (:standard -warn-error -A)))

(menhir
 (modules parser))

(ocamllex lexer)
