(include_subdirs unqualified)

(library
 (name xmas)
 (libraries ppx_show.runtime angstrom core )
 (preprocess
  (pps ppx_show ppx_variants_conv ))
 (flags
  (:standard -warn-error -A)))

(menhir
 (modules parser2))

(ocamllex lexer)
